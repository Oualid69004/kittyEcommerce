<div class="component-example">
  <div class="container" style="margin-top: 250px;">
    <div class="row">
      <div class="col-md-12">
        <figure  class="text-center">
          <a class="lightbox" href="<%=asset_path('kit.jpg')%>" title="Image title">
            <img src="<%=asset_path('kit.jpg')%>" alt="Image">
          </a>
        </figure>
      </div>


    <div class="col-md-12 col-lg-12">
      <div class="card text-center">
        <div class="card-header">
          <%= item.title %>
        </div>
        <div class="card-body">
          <h5 class="card-title"><%= item.title%> </h5>
          <h5 class="card-title"><%= item.description %> </h5>
          <p class="card-text"><%= item.price %>€</p>
          <%if user_signed_in?%>
              <% if current_user.cart.items.include?(item)  %>
              <small class="text-primary">Déjà dans votre panier</small>
              <%else%>
              <%=link_to 'Ajout rapide au panier',  item_itemcart_index_path(item.id), method: :post, class: "btn btn-primary btn-sm"%>
              <% end %>
            <%end%>
        </div>
        <div class="card-footer text-muted">
           days ago
        </div>
      </div>
    </div>
  </div>
</div>
